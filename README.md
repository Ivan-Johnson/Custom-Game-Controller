This branch is kinda-sorta deprecated.

My original plan for this git repo was to create a virtual controller. I'd have
two identical one-handed controllers plugged into my computer (specifically, the
["Xbox Adaptive Joystick"
controller](https://www.xbox.com/en-US/accessories/controllers/xbox-adaptive-joystick)),
which would then be merged into a single virtual controller.

Instead, I'm now planning to make my own physical controller.
